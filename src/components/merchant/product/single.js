import React, { Component } from 'react';
import { Product } from '../../../components';

class Single extends Component {
  render() {
    return <Product.Single id={null} />;
  }
}

export default Single;
